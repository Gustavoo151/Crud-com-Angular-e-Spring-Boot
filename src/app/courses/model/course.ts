export interface Course {  // Interfaca para declarar as variáveis que vão ter no course
  // essa interface são será usada quando compilarmos o programa.
  _id: string;
  name: string;
  category: string;
}
